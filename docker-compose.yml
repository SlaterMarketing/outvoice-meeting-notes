services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: outvoice
      POSTGRES_PASSWORD: outvoice
      POSTGRES_DB: outvoice
    volumes:
      - outvoice_pg:/var/lib/postgresql/data

volumes:
  outvoice_pg:
