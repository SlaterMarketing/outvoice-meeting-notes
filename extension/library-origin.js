/**
 * Your Outvoice web app (origin only: scheme + host, optional port, no path, no trailing slash).
 * Change this when you self-host or use a deployed URL, then reload the extension.
 *
 * Examples:
 *   "http://localhost:3000"
 *   "https://outvoice.example.com"
 */
window.OUTVOICE_LIBRARY_ORIGIN = "http://localhost:3000";
